upload-path: D:/uploadTemplate/
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/court
    username: root
    password: zhouyiran
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  freemarker:
    suffix: .ftl
    cache: false
  http:
    multipart:
      max-file-size: 100Mb
      max-request-size: 1000Mb
  resources:
    static-locations: classpath:/META-INF/resources/,classpath:/resources/,classpath:/static/,classpath:/public/,file:${upload-path}
  mvc:
    static-path-pattern: /**
